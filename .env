# Pusher public credentials for real-time WebSocket updates.
# These are public (embedded in client JS) — not secrets.
# Production builds need these set in the build environment.
# Get values from the Pusher dashboard: https://dashboard.pusher.com
VITE_PUSHER_KEY=8affd7213bb4643ca7f1
VITE_PUSHER_CLUSTER=us2
